version: 2

sources:
  # DEFERRED: RisingWave JDBC external catalog (pending connector fix)
  # Once available, this source will be used for the hot-path in the unified MV:
  #   - name: risingwave
  #     database: risingwave
  #     schema: public
  #     tables:
  #       - name: funnel_summary

  - name: databricks_uc
    database: databricks_uc
    schema: sr_poc_external
    tables:
      - name: funnel_summary_historical

  - name: lakekeeper_local
    database: lakekeeper_local
    schema: public
    tables:
      - name: funnel_summary
