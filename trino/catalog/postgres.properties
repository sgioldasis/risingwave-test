# PostgreSQL Catalog Configuration for Trino
# This allows Trino to query the local PostgreSQL database

connector.name=postgresql
connection-url=jdbc:postgresql://host.docker.internal:5432/postgres
connection-user=postgres
connection-password=

# Reduce metadata caching to pick up schema changes faster
metadata.cache-ttl=10s
metadata.cache-missing=false
