funnel_profile:
  target: dev
  outputs:
    dev:
      type: risingwave
      host: "{{ env_var('DBT_HOST', '127.0.0.1') }}"
      user: root
      password: "{{ env_var('DBT_PASSWORD', 'root') }}"
      dbname: dev
      port: 4566     # RisingWave default port
      schema: public
      threads: 1
